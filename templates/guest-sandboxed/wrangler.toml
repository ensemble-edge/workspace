name = "{{WORKER_NAME}}"
main = "src/index.ts"
compatibility_date = "2026-04-08"
compatibility_flags = ["nodejs_compat"]

[dev]
port = 8789

# Inline the built app bundle so the worker is a single file.
[[rules]]
type = "Text"
globs = ["**/*.bundle.js"]
fallthrough = false
