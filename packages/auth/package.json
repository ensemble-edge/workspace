{
  "name": "@ensemble-edge/auth",
  "version": "0.0.1",
  "description": "Ensemble workspace authentication - JWT, sessions, magic links, OIDC",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ensemble-edge/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  },
  "files": [
    "src"
  ],
  "license": "MIT"
}
